# Implementation Plans

Generated by the improve skill (deep audit) on 2026-09-15, planned at commit
`cb3de7b`. Repo: `cortega26/cortega26` — GitHub profile (ships `README.md` +
`TOOLTICIAN.md`; one stdlib-only script). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done. This file is the
source of truth for per-plan status — update rows here, nowhere else.
For wave sequencing, scoreboard, and backlog, see `ROADMAP.md` (same directory).

Baselines: content planned at `cb3de7b`; the operator then synced `main` to
`origin/main` (`77224fb`, README line-5 link removal only) and committed
`scripts/`, `.codegraph/.gitignore`, and `plans/` verbatim — no excerpt
content changed (verified post-sync). Drift-check bases were advanced to the
custody commit accordingly; "Planned at" keeps the original audit commit.

Local `main` was 1 commit behind `origin/main` (`77224fb`) at plan time —
plan 001 absorbs that sync; later plans assume it.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Commit smoke gate + CI | P1 | S | — | DONE |
| 002 | Smoke robustness (CLI crash, state, fragments, surface check) | P1 | S | 001 | DONE |
| 003 | Stale badge paragraph + EN/ES Monedario parity | P2 | S | 001 | DONE |
| 004 | Remove orphan PNG + AGENTS.md executor guidance | P3 | S | 001 | DONE |
| 005 | Opt-in online link-rot check (weekly schedule) | P2 | M | 001, 002 | DONE |
| 006 | Add chile-hub to Featured Projects (EN+ES) | P2 | S | 001, 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 requires 001 because it modifies the committed script, its new test file, and the workflow 001 creates.
- 003 requires 001 so CI gates the doc edits (content-independent otherwise).
- 004 requires 001 for the same CI-gate reason.
- 005 requires 001 and 002 because it extends the hardened script and its test file.
- 006 requires 001 (CI gate) and 003 (repo-first + parity rule; avoids concurrent edits to the same README lines).
- 002, 003, 004 are mutually independent once 001 lands — but use separate branches per plan, never one shared branch.

## Findings considered and rejected

- Dependency migration / framework upgrades: rejected — zero dependencies, nothing to migrate. (So nobody re-audits it.)
- Performance optimization: rejected — static markdown; the only weight issue (orphan PNG) is plan 004.
- Secrets/credentials audit exposure: investigated, repo is clean (no tokens, keys, or `.env` anywhere) — no plan needed.
- Cross-repo Tooltician badge-compliance checking (audit direction #3): rejected for this repo — spans other repos; noted here so it isn't re-proposed against `github-profile`.
- Automated EN/ES parity gate (audit direction #2): deferred, not planned — needs an editorial strictness decision first; candidate for a future `plan` invocation once the maintainer weighs in.
- Third-party link checkers (lychee action / `requests`) for plan 005: rejected — stdlib `urllib` keeps the repo's zero-dependency contract.
